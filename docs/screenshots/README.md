# Screenshots

Screenshots will be added after the next manual walkthrough pass.

Suggested captures:

- Main UI with pasted text and playback controls
- Speed slider and engine selector
- Terminal output from `./start.sh` showing local URL

Run the app and capture at **http://127.0.0.1:8765/** after `./setup.sh`.
